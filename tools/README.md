# tools

Workspace-level scripts for setup, checks, and cleanup.

Current scripts:

- `setup_base_env.ps1`: installs dependency files manually when needed.
- `check_workspace.py`: verifies scaffold paths.
- `clean_temp_outputs.ps1`: removes temporary output files from scaffold output directories.

