# Manifests

Manifest files describe image paths, labels, sources, splits, and notes.

Use `dataset_manifest_template.csv` as the starting format. Paths should be relative to `vehicle_type_classification`.

Current phase:

- Keep only templates and small text manifests in git.
- Do not commit real image data.

