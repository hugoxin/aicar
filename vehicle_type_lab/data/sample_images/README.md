# Sample Images

This directory is reserved for a few small local sample images used by future demos.

Do not place large datasets here. Large datasets should stay under the shared `datasets` workspace.

