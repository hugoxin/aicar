# datasets

Shared dataset directory for the AI car wash workspace.

Current phase:

- Do not download large datasets.
- Keep raw data, processed data, annotations, and samples separated.
- Do not commit large dataset files to git.

Reserved structure:

- `raw`
- `processed`
- `vehicle_type`
- `annotations`
- `samples`

