# models

Shared model directory for the AI car wash workspace.

Current phase:

- Do not download pretrained models.
- Do not train models.
- Do not commit large model files to git.

Reserved structure:

- `pretrained`
- `trained`
- `exported`

